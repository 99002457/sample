name: Build-Gtest

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
 

    steps:
    - uses: actions/checkout@v2
    - name: Insatll gtest
      run: sudo apt-get install libgtest-dev
    - name: g
      run: sudo apt-get install cmake
    - name: Cmake Gtest
      run: sudo cmake -C /usr/src/gtest/CMakeLists.txt
    - name: Cmake -C /usr/src/gtest/make
      run: sudo make
    - name: Copy
      run: sudo cp *.a /usr/lib
    - name: Configure
      run: mkdir build && cd build  && cmake -DCMAKE_CXX_FLAGS="-Werror" ..
    - name: Build
      run:  cmake --build build
    - name: Test
      run: cd build && ctest
    - name: make
      run: make
